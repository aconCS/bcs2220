import java.util.PriorityQueue;

public class AStar{

  /**
   * So given k actions (eat fish, etc.), calculate next world state and cost.
   * Check that it does not exceed the final cost.
   * Add them all to the open queue as an initial node(s)
   * Tie breaker would be the node with more layers
   */
  public static AStar(){
    PriorityQueue<String[]> open;
    String closed;

    while (!open.isEmpty()) {
      open.add()
    }
  }

  public static AStar(open, closed, running) {
    open would need to store worldState and the cost 
  }
}
