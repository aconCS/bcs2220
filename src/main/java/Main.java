class Main {
  String[][] world = iniitalizeWorld(100, 100, 0, 0);
  int maxDailyCost = 10;
  
  // So-far cost from start to selected point
  // Must be less than maxDailyCost
  int actualCost = 0;

  boolean running = True;
  while(running) {
    running = checkIfGameOn(world);

    
  }
}
